export const SampleCodes = () => {
  // update single object is state array
  /*
  setTodos(todos.map(todo => {
    if(t.id === todo.id){
        t.completed = !t.completed;
        return {...todo, t};
    }
    return todo;
    }));
    */
  // add new item to the state array
  /**
      setNames([...names, newName]);
     */
  // update existing state object
  /**
   type of object is
   object {
    firstName : string;
    lastName : string;
   }
   setObjects({...object, firstName : 'shehan'})
   */
};
